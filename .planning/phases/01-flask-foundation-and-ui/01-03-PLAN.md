---
phase: 01-flask-foundation-and-ui
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - static/script.js
  - templates/base.html
autonomous: true
requirements:
  - UI-03

must_haves:
  truths:
    - "Client-side JavaScript exists for filter interactions (Phase 3 will implement logic)"
    - "Base template properly loads JavaScript file"
    - "JavaScript file structure ready for filter logic addition"
  artifacts:
    - path: "static/script.js"
      provides: "Client-side JavaScript scaffold for filters"
      min_lines: 20
  key_links:
    - from: "templates/base.html"
      to: "static/script.js"
      via: "Script tag with url_for"
      pattern: "url_for.*script.js"
    - from: "static/script.js"
      to: "Filter controls"
      via: "getElementById or querySelector"
      pattern: "getElementById.*Filter"
---

<objective>
Create client-side JavaScript scaffold for filter interactions

**Purpose:** Prepare the JavaScript foundation that will handle filter interactions in Phase 3. This plan sets up the file structure and basic event listeners, making Phase 1 UI complete while deferring actual filter logic to Phase 3.

**Output:** JavaScript file loaded by templates, ready to implement filter logic in next phase.
</objective>

<execution_context>
@/Users/jayliang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jayliang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jayliang/cba-schedule-site/.planning/PROJECT.md
@/Users/jayliang/cba-schedule-site/.planning/ROADMAP.md
@/Users/jayliang/cba-schedule-site/.planning/REQUIREMENTS.md
@/Users/jayliang/cba-schedule-site/cba_script.js
</context>

<tasks>

<task type="auto">
  <name>Create JavaScript scaffold file</name>
  <files>static/script.js</files>
  <action>
Create `static/script.js` with minimal structure:

1. **Document ready handler**: Use `DOMContentLoaded` event
2. **Filter element references**: Get DOM elements for:
   - teamFilter (select)
   - dateFilter (input type="date")
   - statusFilter (select)
   - resetFilters (button)
3. **Event listener stubs**: Add event listeners that currently do nothing (placeholder for Phase 3):
   - teamFilter.addEventListener('change', () => console.log('Team filter changed'))
   - dateFilter.addEventListener('change', () => console.log('Date filter changed'))
   - statusFilter.addEventListener('change', () => console.log('Status filter changed'))
   - resetFilters.addEventListener('click', () => console.log('Reset clicked'))
4. **Refresh handler stub**: Create function for refresh button (if added in template)

DO NOT implement actual filter logic (filterByTeam, filterByDate, filterByStatus functions). Phase 3 will add this.

DO NOT copy the entire cba_script.js file. This is a fresh start with only structure needed for Phase 3 to build on.

Add comments indicating "Phase 3: Implement filter logic here" in appropriate places.

Keep file small (<50 lines) - this is just scaffolding.
  </action>
  <verify>
    <automated>test -f static/script.js && grep -q "DOMContentLoaded" static/script.js && grep -q "addEventListener" static/script.js && echo "PASS: script.js exists with event listeners"</automated>
    <manual>Verify static/script.js has DOM element references and event listener stubs</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>static/script.js exists with DOM ready handler, filter element references, and event listener stubs</done>
</task>

<task type="auto">
  <name>Add JavaScript to base template</name>
  <files>templates/base.html</files>
  <action>
Update `templates/base.html` to load JavaScript:

1. In the `{% block scripts %}` section (or create one if not exists, just before `</body>`):
   - Add script tag: `<script src="{{ url_for('static', filename='script.js') }}"></script>`

2. Ensure script tag is at the end of body (after content block) so DOM is loaded before script runs.

3. Keep the scripts block structure so child templates (like schedule.html) can add additional scripts if needed in future phases.

This completes the static asset loading: CSS in `<head>`, JS at end of `<body>`.
  </action>
  <verify>
    <automated>grep -q "url_for.*script.js" templates/base.html && echo "PASS: base.html loads script.js"</automated>
    <manual>Verify base.html has script tag for script.js in scripts block</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>templates/base.html updated to load static/script.js in scripts block</done>
</task>

<task type="auto">
  <name>Test filter controls render and JavaScript loads</name>
  <files>app.py</files>
  <action>
No changes to app.py needed for this task. This is a verification-focused task.

Verify:
1. Start Flask app with `flask run`
2. Visit http://127.0.0.1:5000/
3. Open browser DevTools console
4. Interact with filters (change team, date, status)
5. Check console output for "Team filter changed", "Date filter changed", "Status filter changed" messages
6. Click reset button - should see "Reset clicked" message

If verification fails, debug:
- Check browser console for JavaScript errors
- Verify element IDs in template match getElementById calls in script.js
- Verify script.js is properly served (check Network tab in DevTools)

This confirms UI layer is complete and ready for Phase 3 filter logic implementation.
  </action>
  <verify>
    <automated>cd /Users/jayliang/cba-schedule-site && python -m flask --app app run --port 5003 & sleep 3 && curl -s http://127.0.0.1:5003/ | grep -q "script.js" && pkill -f "flask.*app run" && echo "PASS: HTML includes script.js reference"</automated>
    <manual>Open browser, interact with filters, verify console.log messages appear</manual>
    <sampling_rate>run after all tasks complete</sampling_rate>
  </verify>
  <done>Filter controls render in HTML, JavaScript loads without errors, event listeners fire on interaction</done>
</task>

</tasks>

<verification>
**Overall phase checks:**
1. Flask app serves static/script.js file successfully
2. Browser console shows no JavaScript errors on page load
3. Filter interactions trigger console.log messages
4. Script tag present in rendered HTML source
5. JavaScript event listeners attached to all filter controls

**Key files created:**
- static/script.js (JavaScript scaffold)
- templates/base.html updated (script tag added)
</verification>

<success_criteria>
**Measurable completion:**
1. static/script.js file exists and is served by Flask
2. Browser DevTools console shows console.log messages when filters are interacted with
3. No JavaScript errors in browser console
4. All filter controls (team, date, status, reset) have event listeners attached
5. Code structure ready for Phase 3 filter logic implementation
</success_criteria>

<output>
After completion, create `.planning/phases/01-flask-foundation-and-ui/01-03-SUMMARY.md`
</output>
