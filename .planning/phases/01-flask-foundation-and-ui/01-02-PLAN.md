---
phase: 01-flask-foundation-and-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/base.html
  - templates/schedule.html
  - static/style.css
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-05

must_haves:
  truths:
    - "Jinja2 template renders schedule table with team names, dates, and statuses"
    - "Filter controls (date picker, team dropdown, status selector) are present in HTML"
    - "Page displays correctly on desktop with full table layout"
    - "Page displays correctly on mobile with card layout at <768px breakpoint"
  artifacts:
    - path: "templates/base.html"
      provides: "Base Jinja2 template with layout structure"
      min_lines: 30
    - path: "templates/schedule.html"
      provides: "Schedule table template extending base"
      min_lines: 40
    - path: "static/style.css"
      provides: "Responsive styling adapted from current CSS"
      min_lines: 200
  key_links:
    - from: "templates/schedule.html"
      to: "templates/base.html"
      via: "Jinja2 extends directive"
      pattern: "{% extends"
    - from: "templates/schedule.html"
      to: "static/style.css"
      via: "Link tag in base template"
      pattern: "link.*style.css"
    - from: "templates/schedule.html"
      to: "Flask route"
      via: "render_template in app.py"
      pattern: "render_template.*schedule"
---

<objective>
Create Jinja2 templates and responsive styling for schedule display

**Purpose:** Transform current vanilla HTML/CSS into Jinja2 templates that render schedule data server-side, maintaining all current UI features including filters, responsive design, and styling.

**Output:** Working templates that render schedule table with filter controls, styled consistently with current site, responsive on mobile and desktop.
</objective>

<execution_context>
@/Users/jayliang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jayliang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jayliang/cba-schedule-site/.planning/PROJECT.md
@/Users/jayliang/cba-schedule-site/.planning/ROADMAP.md
@/Users/jayliang/cba-schedule-site/.planning/REQUIREMENTS.md
@/Users/jayliang/cba-schedule-site/cba_schedule.html
@/Users/jayliang/cba-schedule-site/cba_style.css
@/Users/jayliang/cba-schedule-site/cba_script.js
</context>

<tasks>

<task type="auto">
  <name>Create Jinja2 base template</name>
  <files>templates/base.html</files>
  <action>
Create `templates/base.html` with:
- HTML5 doctype and structure
- Meta tags (charset UTF-8, viewport for mobile)
- Title block using Jinja2 `{% block title %}` with default "CBA赛程查询 - 2025-26赛季"
- Link to static CSS: `{{ url_for('static', filename='style.css') }}`
- Body with header (`<h1>CBA篮球联赛赛程查询</h1>`)
- Subtitle line: "2025-26赛季 | 数据来源: 新浪体育 | <a href='...'>官方数据</a>"
- Content block: `{% block content %}{% endblock %}`
- Scripts block: `{% block scripts %}{% endblock %}`

Structure mirrors current `cba_schedule.html` but uses Jinja2 template inheritance pattern.

DO NOT include JavaScript inline filtering logic yet (Phase 3). Keep templates server-rendered only for now.
  </action>
  <verify>
    <automated>test -f templates/base.html && grep -q "{% block content %}" templates/base.html && grep -q "url_for" templates/base.html && echo "PASS: base.html contains Jinja2 blocks and url_for"</automated>
    <manual>Verify base.html has proper HTML5 structure with Jinja2 template blocks</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>templates/base.html exists with Jinja2 template structure, CSS link, and content blocks</done>
</task>

<task type="auto">
  <name>Create schedule table template</name>
  <files>templates/schedule.html</files>
  <action>
Create `templates/schedule.html` extending base.html:
- `{% extends "base.html" %}`
- Override title block if needed
- In content block:
  - Refresh button (form POST to same route or button with htmx/fetch later)
  - Filter section div with:
    - Team filter: `<select name="team">` populated with `{% for team in teams %}` from Flask context
    - Date filter: `<input type="date" name="date">`
    - Status filter: `<select name="status">` with options (全部, 未开始, 进行中, 已结束)
    - Reset button
  - Schedule container div with table:
    - Table headers: 轮次, 日期, 时间, 主队, 比分, 客队, 场地, 状态, 操作
    - Table body: `{% for game in schedule %}` loop rendering rows
    - Each row: game.round, game.date, game.time, game.homeTeam, score display, game.awayTeam, game.venue, game.status, action buttons
    - Use `data-label` attributes on `<td>` elements for mobile card layout CSS (matches current structure)

Pass dummy schedule data from Flask route context (empty list or single sample game for now).

Important: Keep filter controls as UI-only for now. Actual filtering logic will be added in Phase 3. This task focuses on rendering the HTML structure.
  </action>
  <verify>
    <automated>test -f templates/schedule.html && grep -q "{% extends" templates/schedule.html && grep -q "{% for game in schedule %}" templates/schedule.html && echo "PASS: schedule.html extends base and loops over schedule"</automated>
    <manual>Verify schedule.html has filter controls and table structure with Jinja2 loops</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>templates/schedule.html exists with filter controls, schedule table structure using Jinja2 loops, and mobile-friendly data-label attributes</done>
</task>

<task type="auto">
  <name>Adapt responsive CSS for Flask templates</name>
  <files>static/style.css</files>
  <action>
Copy content from current `cba_style.css` to `static/style.css` with minimal changes:
- Keep all existing styles (font, table, filter section, responsive design)
- Maintain the @media query for mobile (max-width: 768px)
- Keep all class names unchanged (.filter-section, .status.ended, .status.live, etc.)
- Ensure mobile card layout styles work with Jinja2-generated HTML
- Add any missing styles for Jinja2 template elements if needed

DO NOT change styling significantly - goal is to match current visual appearance exactly.

Verify responsive breakpoint at 768px still works:
- Desktop: Full table layout with horizontal columns
- Mobile: Vertical card layout with data-label pseudo-elements showing column headers
  </action>
  <verify>
    <automated>test -f static/style.css && grep -q "@media screen and (max-width: 768px)" static/style.css && grep -q "filter-section" static/style.css && echo "PASS: style.css contains responsive media query and filter styles"</automated>
    <manual>Verify static/style.css has responsive mobile styles and all current design classes</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>static/style.css exists with all current styling, responsive design intact, and compatible with Jinja2 template structure</done>
</task>

<task type="auto">
  <name>Update Flask route to render schedule template</name>
  <files>app.py</files>
  <action>
Update the GET / route in `app.py` to:
- Import `render_template` from flask
- Create sample schedule data (list of 2-3 game dictionaries with keys: round, date, time, homeTeam, awayTeam, venue, status, homeScore, awayScore)
- Create teams list from CBA_TEAMS constant
- Call `render_template('schedule.html', schedule=sample_schedule, teams=CBA_TEAMS)`
- Return rendered template

Sample game format (matching current structure):
```python
{
    'id': 1,
    'round': '第1轮',
    'date': '2025-10-20',
    'time': '19:35',
    'homeTeam': '广东',
    'awayTeam': '辽宁',
    'venue': '东莞篮球中心',
    'status': '未开始',
    'homeScore': None,
    'awayScore': None,
    'weekDay': '周一'
}
```

This demonstrates template rendering. Real data will come from Phase 2.
  </action>
  <verify>
    <automated>cd /Users/jayliang/cba-schedule-site && python -m flask --app app run --port 5002 & sleep 3 && curl -s http://127.0.0.1:5002/ | grep -q "广东" && pkill -f "flask.*app run" && echo "PASS: Flask renders schedule template with sample data"</automated>
    <manual>Visit http://127.0.0.1:5000/ and verify schedule table renders with sample games</manual>
    <sampling_rate>run after all tasks complete</sampling_rate>
  </verify>
  <done>app.py updated to render schedule.html with sample schedule data and teams list</done>
</task>

</tasks>

<verification>
**Overall phase checks:**
1. Start Flask app - templates render without Jinja2 syntax errors
2. Visit / - schedule table displays with sample data
3. Check filter controls - all three filters (team, date, status) present and styled
4. Resize browser to <768px - table switches to card layout
5. Check CSS - styling matches current site appearance

**Key files created:**
- templates/base.html (base template)
- templates/schedule.html (schedule page)
- static/style.css (responsive styling)
- app.py updated (renders templates)
</verification>

<success_criteria>
**Measurable completion:**
1. Jinja2 templates render without syntax errors
2. Schedule table displays with all columns (轮次, 日期, 时间, 主队, 比分, 客队, 场地, 状态, 操作)
3. Filter controls render with proper styling from CSS
4. Mobile responsive design works at 768px breakpoint (desktop table → mobile cards)
5. Visual appearance matches current cba_schedule.html styling
</success_criteria>

<output>
After completion, create `.planning/phases/01-flask-foundation-and-ui/01-02-SUMMARY.md`
</output>
