---
phase: 01-flask-foundation-and-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.py
  - requirements.txt
  - vercel.json
  - .gitignore
autonomous: true
requirements:
  - FRAME-01
  - FRAME-02

must_haves:
  truths:
    - "Flask app starts without errors when running `flask run`"
    - "GET / endpoint returns HTTP 200 response"
    - "Flask application structure follows serverless-compatible patterns"
  artifacts:
    - path: "app.py"
      provides: "Flask application with routes"
      min_lines: 30
    - path: "requirements.txt"
      provides: "Python dependencies"
      contains: "Flask"
    - path: "vercel.json"
      provides: "Vercel serverless deployment config"
      contains: "python"
  key_links:
    - from: "app.py"
      to: "Flask framework"
      via: "from flask import Flask"
      pattern: "from flask import"
    - from: "vercel.json"
      to: "app.py"
      via: "WSGI application reference"
      pattern: "app:app"
---

<objective>
Bootstrap Flask application structure with serverless-compatible entry point

**Purpose:** Establish the foundational Flask application that can serve both HTML pages and JSON API endpoints from a single codebase, compatible with Vercel serverless deployment.

**Output:** Working Flask app with homepage route, dependency management, and deployment configuration.
</objective>

<execution_context>
@/Users/jayliang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jayliang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jayliang/cba-schedule-site/.planning/PROJECT.md
@/Users/jayliang/cba-schedule-site/.planning/ROADMAP.md
@/Users/jayliang/cba-schedule-site/.planning/REQUIREMENTS.md
@/Users/jayliang/cba-schedule-site/.planning/codebase/STACK.md
@/Users/jayliang/cba-schedule-site/.planning/codebase/STRUCTURE.md
@/Users/jayliang/cba-schedule-site/.planning/codebase/ARCHITECTURE.md
@/Users/jayliang/cba-schedule-site/cba_script.js
</context>

<tasks>

<task type="auto">
  <name>Create Flask application entry point</name>
  <files>app.py</files>
  <action>
Create `app.py` with:
- Flask app initialization (`app = Flask(__name__)`)
- Configure static folder path for CSS (`static_folder='static'`)
- Configure templates folder (`template_folder='templates'`)
- Add GET / route that returns "CBA Schedule Flask - Coming Soon" placeholder HTML
- Add CORS headers for future API endpoints (use `after_request` decorator)
- Add basic error handling (404, 500)
- Export `app` for WSGI compatibility with Vercel

Use embedded dummy schedule data from current `cba_script.js` constants (CBA_TEAMS, TEAM_FULL_NAMES, TEAM_VENUES) as Python dictionaries in app.py for now. This data will be used by Jinja2 templates in Plan 02.

Important: DO NOT create database connections or complex state management. Keep it simple and serverless-compatible (stateless, cold-start friendly).
  </action>
  <verify>
    <automated>cd /Users/jayliang/cba-schedule-site && python -m flask --app app run --port 5001 & sleep 3 && curl -s http://127.0.0.1:5001/ | grep -q "CBA" && pkill -f "flask.*app run" && echo "PASS: Flask app responds on /"</automated>
    <manual>Verify app.py contains Flask initialization, route definitions, and WSGI export</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>app.py exists with Flask app initialization, GET / route returning HTML, CORS headers configured, and WSGI export for Vercel</done>
</task>

<task type="auto">
  <name>Create Python dependencies file</name>
  <files>requirements.txt</files>
  <action>
Create `requirements.txt` with minimal dependencies:
- Flask==3.0.2 (current stable)
- Gunicorn==21.2.0 (WSGI server for production, though Vercel uses its own)

DO NOT add database drivers, unnecessary libraries, or anything not immediately needed for Phase 1.

Note: iconv-lite equivalent in Python is built-in (no need for extra encoding libraries until Phase 2 data scraping).
  </action>
  <verify>
    <automated>test -f requirements.txt && grep -q "Flask" requirements.txt && grep -q "Gunicorn" requirements.txt && echo "PASS: requirements.txt contains Flask and Gunicorn"</automated>
    <manual>Verify requirements.txt has exactly 2 lines (Flask and Gunicorn with versions)</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>requirements.txt exists with Flask 3.x and Gunicorn specified</done>
</task>

<task type="auto">
  <name>Configure Vercel deployment for Flask</name>
  <files>vercel.json, .gitignore</files>
  <action>
Update `vercel.json` to configure Vercel for Flask application:
- Set `builds` to use `@vercel/python` runtime
- Set `routes` to forward all requests to Flask WSGI app
- Remove old API endpoint rewrites (will be replaced by Flask routes)
- Add environment configuration for Python 3.11

Create/update `.gitignore` to exclude:
- `__pycache__/`
- `*.pyc`
- `*.pyo`
- `*.pyd`
- `.Python`
- `env/`
- `venv/`
- `.venv/`
- `instance/`
- `.pytest_cache/`
- Keep existing `node_modules/` exclusion

Reference existing vercel.json structure but adapt for Python/Flask instead of Node.js API routes.
  </action>
  <verify>
    <automated>test -f vercel.json && grep -q "@vercel/python" vercel.json && test -f .gitignore && grep -q "__pycache__" .gitignore && echo "PASS: Vercel config and gitignore updated"</automated>
    <manual>Verify vercel.json uses Python runtime and .gitignore excludes Python artifacts</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>vercel.json configured for Python/Flask deployment, .gitignore excludes Python artifacts</done>
</task>

</tasks>

<verification>
**Overall phase checks:**
1. Run `flask --app app run` - should start without errors
2. Visit http://127.0.0.1:5000/ - should return placeholder HTML
3. Check `requirements.txt` - should contain Flask and Gunicorn
4. Check `vercel.json` - should reference Python runtime
5. Python artifacts properly excluded in `.gitignore`

**Key files created:**
- app.py (Flask application)
- requirements.txt (dependencies)
- vercel.json (deployment config)
- .gitignore (updated)
</verification>

<success_criteria>
**Measurable completion:**
1. Flask app starts successfully with `flask run` command
2. GET / returns HTTP 200 with placeholder HTML content
3. No import errors or missing dependencies
4. Vercel configuration valid for Python deployment
5. All Python artifacts properly excluded from git
</success_criteria>

<output>
After completion, create `.planning/phases/01-flask-foundation-and-ui/01-01-SUMMARY.md`
</output>
